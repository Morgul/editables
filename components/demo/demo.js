//----------------------------------------------------------------------------------------------------------------------
// Setup for the demo page.
//
// @module demo.js
//----------------------------------------------------------------------------------------------------------------------

angular.module('demoApp', ['editables']).run(function($rootScope)
{
    $rootScope.msgs = [];

    $rootScope.save = function(name)
    {
        $rootScope.msgs.push(name + " successfully saved.");
    }; // end save
});

//----------------------------------------------------------------------------------------------------------------------